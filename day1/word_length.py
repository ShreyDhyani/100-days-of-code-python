word = input("Enter a word to count its length.\n").strip();
# Using , for seperation
print("Word length is ->", len(word));
# Using string conversion
print("Word length is -> "+ str(len(word)));
# Using f function conversion for string
print(f"Word length is -> {len(word)}");
